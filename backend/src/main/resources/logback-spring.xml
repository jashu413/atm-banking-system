<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!--
      Dev profile: human-readable console output.
      Activated when spring.profiles.active=dev (or includes "dev").
    -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %-40.40logger{39} - %msg%n</pattern>
            </encoder>
        </appender>

        <logger name="com.bank" level="DEBUG"/>
        <logger name="org.hibernate.SQL" level="DEBUG"/>
        <logger name="org.hibernate.orm.jdbc.bind" level="TRACE"/>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!--
      All other profiles (staging, prod, default): structured JSON via logstash-logback-encoder.
      Log format is suitable for Elasticsearch, Loki, CloudWatch Logs, and similar ingestors.

      Security note: passwords, PINs, JWTs, and raw refresh tokens must NEVER appear in log
      messages. Services log only usernames, account numbers, and action outcomes.
    -->
    <springProfile name="!dev">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeContext>false</includeContext>
                <fieldNames>
                    <timestamp>timestamp</timestamp>
                    <message>message</message>
                    <logger>logger</logger>
                    <thread>thread</thread>
                    <level>level</level>
                    <stackTrace>stack_trace</stackTrace>
                </fieldNames>
                <throwableConverter class="net.logstash.logback.stacktrace.ShortenedThrowableConverter">
                    <maxDepthPerCause>20</maxDepthPerCause>
                    <rootCauseFirst>true</rootCauseFirst>
                    <exclude>sun\.reflect\..*</exclude>
                    <exclude>java\.lang\.reflect\..*</exclude>
                </throwableConverter>
            </encoder>
        </appender>

        <!-- Suppress noisy Hibernate SQL in non-dev -->
        <logger name="org.hibernate.SQL" level="OFF"/>
        <logger name="org.hibernate.orm.jdbc.bind" level="OFF"/>
        <!-- Suppress Spring framework debug noise -->
        <logger name="org.springframework.web" level="WARN"/>
        <logger name="org.springframework.security" level="WARN"/>

        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
    </springProfile>

</configuration>
